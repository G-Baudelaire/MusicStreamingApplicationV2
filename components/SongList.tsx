import {FlatList} from "react-native";

export default class SongList extends FlatList {
    // constructor() {
    //     super();
    // }
    // return (
    //     <FlatList
    //         data={songs}
    //         renderItem={({item}) => <Item title={item.title}/>}
    //         keyExtractor={item => item.id}
    //     />
    // )
}

